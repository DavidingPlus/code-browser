<def f='include/freetype2/freetype/ftimage.h' l='119' ll='124'/>
<use f='include/freetype2/freetype/ftimage.h' l='119'/>
<size>32</size>
<doc f='include/freetype2/freetype/ftimage.h' l='84'>/**************************************************************************
   *
   * @struct:
   *   FT_BBox
   *
   * @description:
   *   A structure used to hold an outline&apos;s bounding box, i.e., the
   *   coordinates of its extrema in the horizontal and vertical directions.
   *
   * @fields:
   *   xMin ::
   *     The horizontal minimum (left-most).
   *
   *   yMin ::
   *     The vertical minimum (bottom-most).
   *
   *   xMax ::
   *     The horizontal maximum (right-most).
   *
   *   yMax ::
   *     The vertical maximum (top-most).
   *
   * @note:
   *   The bounding box is specified with the coordinates of the lower left
   *   and the upper right corner.  In PostScript, those values are often
   *   called (llx,lly) and (urx,ury), respectively.
   *
   *   If `yMin` is negative, this value gives the glyph&apos;s descender.
   *   Otherwise, the glyph doesn&apos;t descend below the baseline.  Similarly,
   *   if `ymax` is positive, this value gives the glyph&apos;s ascender.
   *
   *   `xMin` gives the horizontal distance from the glyph&apos;s origin to the
   *   left edge of the glyph&apos;s bounding box.  If `xMin` is negative, the
   *   glyph extends to the left of the origin.
   */</doc>
<mbr r='FT_BBox_::xMin' o='0' t='FT_Pos'/>
<mbr r='FT_BBox_::yMin' o='64' t='FT_Pos'/>
<mbr r='FT_BBox_::xMax' o='128' t='FT_Pos'/>
<mbr r='FT_BBox_::yMax' o='192' t='FT_Pos'/>
