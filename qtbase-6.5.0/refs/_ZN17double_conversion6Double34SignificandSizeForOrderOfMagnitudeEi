<def f='qtbase-6.5.0/src/3rdparty/double-conversion/double-conversion/ieee.h' l='228' ll='234' type='static int double_conversion::Double::SignificandSizeForOrderOfMagnitude(int order)'/>
<doc f='qtbase-6.5.0/src/3rdparty/double-conversion/double-conversion/ieee.h' l='222'>// Returns the significand size for a given order of magnitude.
  // If v = f*2^e with 2^p-1 &lt;= f &lt;= 2^p then p+e is v&apos;s order of magnitude.
  // This function returns the number of significant binary digits v will have
  // once it&apos;s encoded into a double. In almost all cases this is equal to
  // kSignificandSize. The only exceptions are denormals. They start with
  // leading zeroes and their effective significand-size is hence smaller.</doc>
<use f='qtbase-6.5.0/src/3rdparty/double-conversion/double-conversion/strtod.cc' l='334' u='c' c='_ZN17double_conversionL11DiyFpStrtodENS_6VectorIKcEEiPd'/>
