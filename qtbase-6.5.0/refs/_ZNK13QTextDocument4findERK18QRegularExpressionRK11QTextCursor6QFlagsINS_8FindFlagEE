<dec f='qtbase-6.5.0/src/gui/text/qtextdocument.h' l='152' type='QTextCursor QTextDocument::find(const QRegularExpression &amp; expr, const QTextCursor &amp; cursor, QTextDocument::FindFlags options = QTextDocument::FindFlags()) const'/>
<def f='qtbase-6.5.0/src/gui/text/qtextdocument.cpp' l='1539' ll='1549' type='QTextCursor QTextDocument::find(const QRegularExpression &amp; expr, const QTextCursor &amp; cursor, QTextDocument::FindFlags options = QTextDocument::FindFlags()) const'/>
<doc f='qtbase-6.5.0/src/gui/text/qtextdocument.cpp' l='1520'>/*!
    \since 5.5

    Finds the next occurrence that matches the given regular expression,
    \a expr, within the same paragraph in the document.

    The search starts at the position of the given \a cursor, and proceeds
    forwards through the document unless specified otherwise in the search
    options. The \a options control the type of search performed.

    Returns a cursor with the match selected if a match was found; otherwise
    returns a null cursor.

    If the given \a cursor has a selection, the search begins after the
    selection; otherwise it begins at the cursor&apos;s position.

    By default the search is case insensitive, and can match text anywhere in the
    document.
*/</doc>
<use f='qtbase-6.5.0/src/widgets/widgets/qwidgettextcontrol.cpp' l='3158' u='c' c='_ZN18QWidgetTextControl4findERK18QRegularExpression6QFlagsIN13QTextDocument8FindFlagEE'/>
