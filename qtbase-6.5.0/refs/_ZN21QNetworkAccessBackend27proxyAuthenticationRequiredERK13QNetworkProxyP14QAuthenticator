<dec f='qtbase-6.5.0/src/network/access/qnetworkaccessbackend_p.h' l='125' type='void QNetworkAccessBackend::proxyAuthenticationRequired(const QNetworkProxy &amp; proxy, QAuthenticator * auth)'/>
<def f='qtbase-6.5.0/src/network/access/qnetworkaccessbackend.cpp' l='733' ll='740' type='void QNetworkAccessBackend::proxyAuthenticationRequired(const QNetworkProxy &amp; proxy, QAuthenticator * authenticator)'/>
<doc f='qtbase-6.5.0/src/network/access/qnetworkaccessbackend.cpp' l='722'>/*!
    Call this slot if, when connecting through a proxy, it requests
    authentication. This may cause the
    QNetworkAccessManager::proxyAuthenticationRequired() signal to be
    emitted if the credentials are not already stored in an internal
    cache.
    To be able to make the lookup in the cache and potentially the
    subsequent request the \a proxy needs to be known. The credentials
    will be stored in \a authenticator. While \a authenticator is a
    pointer, passing \c nullptr is invalid.
*/</doc>
