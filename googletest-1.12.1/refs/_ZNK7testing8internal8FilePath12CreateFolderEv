<dec f='googletest-1.12.1/googletest/include/gtest/internal/gtest-filepath.h' l='152' type='bool testing::internal::FilePath::CreateFolder() const'/>
<doc f='googletest-1.12.1/googletest/include/gtest/internal/gtest-filepath.h' l='148'>// Create the directory so that path exists. Returns true if successful or
  // if the directory already exists; returns false if unable to create the
  // directory for any reason, including if the parent directory does not
  // exist. Not named &quot;CreateDirectory&quot; because that&apos;s a macro on Windows.</doc>
<use f='googletest-1.12.1/googletest/src/gtest-filepath.cc' l='311' u='c' c='_ZNK7testing8internal8FilePath28CreateDirectoriesRecursivelyEv'/>
<def f='googletest-1.12.1/googletest/src/gtest-filepath.cc' l='318' ll='337' type='bool testing::internal::FilePath::CreateFolder() const'/>
<doc f='googletest-1.12.1/googletest/src/gtest-filepath.cc' l='314'>// Create the directory so that path exists. Returns true if successful or
// if the directory already exists; returns false if unable to create the
// directory for any reason, including if the parent directory does not
// exist. Not named &quot;CreateDirectory&quot; because that&apos;s a macro on Windows.</doc>
