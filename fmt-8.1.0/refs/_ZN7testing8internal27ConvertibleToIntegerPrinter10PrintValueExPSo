<def f='fmt-8.1.0/test/gtest/gtest/gtest.h' l='5309' ll='5311' type='static void testing::internal::ConvertibleToIntegerPrinter::PrintValue(internal::BiggestInt value, ::std::ostream * os)'/>
<doc f='fmt-8.1.0/test/gtest/gtest/gtest.h' l='5302'>// Since T has no &lt;&lt; operator or PrintTo() but can be implicitly
  // converted to BiggestInt, we print it as a BiggestInt.
  //
  // Most likely T is an enum type (either named or unnamed), in which
  // case printing it as an integer is the desired behavior.  In case
  // T is not an enum, printing it as an integer is the best we can do
  // given that it has no user-defined printer.</doc>
