<def f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/bigint.hpp' l='41' type='const size_t'/>
<use f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/bigint.hpp' l='42' u='r'/>
<doc f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/bigint.hpp' l='37'>// number of bits in a bigint. this needs to be at least the number
// of bits required to store the largest bigint, which is
// `log2(10**(digits + max_exp))`, or `log2(10**(767 + 342))`, or
// ~3600 bits, so we round to 4000.</doc>
