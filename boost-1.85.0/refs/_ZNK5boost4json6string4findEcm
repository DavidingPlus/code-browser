<def f='boost-1.85.0/libs/json/include/boost/json/string.hpp' l='2474' ll='2480' type='std::size_t boost::json::string::find(char ch, std::size_t pos = 0) const'/>
<doc f='boost-1.85.0/libs/json/include/boost/json/string.hpp' l='2455'>/** Find the first occurrence of a character within the string.

        Returns the index corrosponding to the first
        occurrence of `ch` within `{begin() + pos, end())`
        if it exists, and @ref npos otherwise.

        @par Complexity

        Linear.

        @return The first occurrence of `ch` within the
        string starting at the index `pos`, or @ref npos
        if none exists.

        @param ch The character to search for.

        @param pos The index to start searching at.
        The default argument for this parameter is `0`.
    */</doc>
