<def f='boost-1.85.0/libs/url/include/boost/url/param.hpp' l='725' ll='732' type='void boost::urls::param_pct_view::param_pct_view(boost::urls::pct_string_view key, boost::urls::pct_string_view value)'/>
<doc f='boost-1.85.0/libs/url/include/boost/url/param.hpp' l='691'>/** Constructor

        This constructs a parameter with a key
        and value, which may both contain percent
        escapes.
        The new key and value reference
        the same corresponding underlying
        character buffers.
        Ownership of the buffers is not transferred;
        the caller is responsible for ensuring that
        the assigned buffers remain valid until
        they are no longer referenced.

        @par Example
        @code
        param_pct_view qp( &quot;key&quot;, &quot;value&quot; );
        @endcode

        @par Postconditions
        @code
        this-&gt;key.data() == key.data() &amp;&amp; this-&gt;value.data() == value.data() &amp;&amp; this-&gt;has_value == true
        @endcode

        @par Complexity
        Linear in `key.size() + value.size()`.

        @par Exception Safety
        Exceptions thrown on invalid input.

        @throw system_error
        `key` or `value` contains an invalid percent-encoding.

        @param key, value The key and value to set.
    */</doc>
<use f='boost-1.85.0/libs/url/src/detail/params_iter_impl.cpp' l='220' u='c' c='_ZNK5boost4urls6detail16params_iter_impl11dereferenceEv'/>
<use f='boost-1.85.0/libs/url/src/params_encoded_ref.cpp' l='210' u='c' c='_ZN5boost4urls18params_encoded_ref3setENS0_15pct_string_viewES2_NS0_17ignore_case_paramE'/>
