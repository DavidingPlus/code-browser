<def f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/bigint.hpp' l='468' ll='485' type='int boost::charconv::detail::fast_float::bigint::compare(const boost::charconv::detail::fast_float::bigint &amp; other) const'/>
<use f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/digit_comparison.hpp' l='390' u='c' c='_ZN5boost8charconv6detail10fast_float19negative_digit_compERNS2_6bigintENS2_17adjusted_mantissaEi'/>
<doc f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/bigint.hpp' l='462'>// compare two big integers, returning the large value.
  // assumes both are normalized. if the return value is
  // negative, other is larger, if the return value is
  // positive, this is larger, otherwise they are equal.
  // the limbs are stored in little-endian order, so we
  // must compare the limbs in ever order.</doc>
