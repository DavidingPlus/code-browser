<dec f='include/unicode/unistr.h' l='3673' type='UBool icu_66::UnicodeString::cloneArrayIfNeeded(int32_t newCapacity = -1, int32_t growCapacity = -1, UBool doCopyArray = 1, int32_t ** pBufferToDelete = 0, UBool forceClone = 0)'/>
<doc f='include/unicode/unistr.h' l='3662'>/*
   * This function is called when write access to the array
   * is necessary.
   *
   * We need to make a copy of the array if
   * the buffer is read-only, or
   * the buffer is refCounted (shared), and refCount&gt;1, or
   * the buffer is too small.
   *
   * Return FALSE if memory could not be allocated.
   */</doc>
