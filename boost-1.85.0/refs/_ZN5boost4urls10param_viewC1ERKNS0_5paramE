<def f='boost-1.85.0/libs/url/include/boost/url/param.hpp' l='531' ll='538' type='void boost::urls::param_view::param_view(const boost::urls::param &amp; other)'/>
<doc f='boost-1.85.0/libs/url/include/boost/url/param.hpp' l='501'>/** Constructor

        This function constructs a param
        which references the character buffers
        representing the key and value in another
        container.
        Ownership of the buffers is not transferred;
        the caller is responsible for ensuring that
        the assigned buffers remain valid until
        they are no longer referenced.

        @par Example
        @code
        param qp( &quot;key&quot;, &quot;value&quot; );
        param_view qpv( qp );
        @endcode

        @par Postconditions
        @code
        this-&gt;key == key &amp;&amp; this-&gt;value == value &amp;&amp; this-&gt;has_value == other.has_value
        @endcode

        @par Complexity
        Constant.

        @par Exception Safety
        Throws nothing.

        @param other The param to reference
    */</doc>
