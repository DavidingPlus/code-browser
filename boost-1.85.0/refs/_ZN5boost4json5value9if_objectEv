<def f='boost-1.85.0/libs/json/include/boost/json/value.hpp' l='2053' ll='2059' type='boost::json::object * boost::json::value::if_object()'/>
<doc f='boost-1.85.0/libs/json/include/boost/json/value.hpp' l='2034'>/** Return an @ref object pointer if this is an object, else return `nullptr`

        If `this-&gt;kind() == kind::object`, returns a pointer
        to the underlying object. Otherwise, returns `nullptr`.

        @par Example
        The return value is used in both a boolean context and
        to assign a variable:
        @code
        if( auto p = jv.if_object() )
            return *p;
        @endcode

        @par Complexity
        Constant.

        @par Exception Safety
        No-throw guarantee.
    */</doc>
