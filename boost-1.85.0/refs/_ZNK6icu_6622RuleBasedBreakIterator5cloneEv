<dec f='include/unicode/rbbi.h' l='277' type='icu_66::RuleBasedBreakIterator * icu_66::RuleBasedBreakIterator::clone() const'/>
<doc f='include/unicode/rbbi.h' l='267'>/**
     * Returns a newly-constructed RuleBasedBreakIterator with the same
     * behavior, and iterating over the same text, as this one.
     * Differs from the copy constructor in that it is polymorphic, and
     * will correctly clone (copy) a derived class.
     * clone() is thread safe.  Multiple threads may simultaneously
     * clone the same source break iterator.
     * @return a newly-constructed RuleBasedBreakIterator
     * @stable ICU 2.0
     */</doc>
