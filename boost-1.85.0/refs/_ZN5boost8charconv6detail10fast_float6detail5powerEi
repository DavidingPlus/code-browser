<def f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/decimal_to_binary.hpp' l='65' ll='67' type='int32_t boost::charconv::detail::fast_float::detail::power(int32_t q)'/>
<use f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/decimal_to_binary.hpp' l='79' u='c' c='_ZN5boost8charconv6detail10fast_float20compute_error_scaledElmi'/>
<use f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/decimal_to_binary.hpp' l='140' u='c' c='_ZN5boost8charconv6detail10fast_float13compute_floatElm'/>
<doc f='boost-1.85.0/libs/charconv/include/boost/charconv/detail/fast_float/decimal_to_binary.hpp' l='50'>/**
 * For q in (0,350), we have that
 *  f = (((152170 + 65536) * q ) &gt;&gt; 16);
 * is equal to
 *   floor(p) + q
 * where
 *   p = log(5**q)/log(2) = q * log(5)/log(2)
 *
 * For negative values of q in (-400,0), we have that
 *  f = (((152170 + 65536) * q ) &gt;&gt; 16);
 * is equal to
 *   -ceil(p) + q
 * where
 *   p = log(5**-q)/log(2) = -q * log(5)/log(2)
 */</doc>
