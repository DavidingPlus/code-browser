<def f='boost-1.85.0/libs/json/include/boost/json/object.hpp' l='1520' ll='1527' type='bool boost::json::operator!=(const boost::json::object &amp; lhs, const boost::json::object &amp; rhs)'/>
<doc f='boost-1.85.0/libs/json/include/boost/json/object.hpp' l='1507'>/** Return `true` if two objects are not equal.

        Objects are equal when their sizes are the same,
        and when for each key in `lhs` there is a matching
        key in `rhs` with the same value.

        @par Complexity
        Constant, or linear (worst case quadratic) in `lhs.size()`.

        @par Exception Safety
        No-throw guarantee.
    */
    // inline friend speeds up overload resolution</doc>
