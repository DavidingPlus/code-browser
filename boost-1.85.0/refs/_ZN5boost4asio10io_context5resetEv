<dec f='boost-1.85.0/libs/asio/include/boost/asio/io_context.hpp' l='533' type='void boost::asio::io_context::reset()'/>
<def f='boost-1.85.0/libs/asio/include/boost/asio/impl/io_context.hpp' l='112' ll='115' type='void boost::asio::io_context::reset()'/>
<doc f='boost-1.85.0/libs/asio/include/boost/asio/io_context.hpp' l='523'>/**
   * This function must be called prior to any second or later set of
   * invocations of the run(), run_one(), poll() or poll_one() functions when a
   * previous invocation of these functions returned due to the io_context
   * being stopped or running out of work. After a call to restart(), the
   * io_context object&apos;s stopped() function will return @c false.
   *
   * This function must not be called while there are any unfinished calls to
   * the run(), run_one(), poll() or poll_one() functions.
   */</doc>
