<dec f='boost-1.85.0/libs/url/include/boost/url/params_ref.hpp' l='811' type='boost::urls::params_base::iterator boost::urls::params_ref::unset(boost::urls::params_base::iterator pos)'/>
<doc f='boost-1.85.0/libs/url/include/boost/url/params_ref.hpp' l='780'>/** Remove the value on an element

        This function removes the value of
        an element at the specified position.
        After the call returns, `has_value`
        for the element is false.

        &lt;br&gt;
        All iterators that are equal to
        `pos` or come after are invalidated.

        @par Example
        @code
        url u( &quot;?first=John&amp;last=Doe&quot; );

        u.params().unset( u.params().begin() );

        assert( u.encoded_query() == &quot;first&amp;last=Doe&quot; );
        @endcode

        @par Complexity
        Linear in `this-&gt;url().encoded_query().size()`.

        @par Exception Safety
        Throws nothing.

        @return An iterator to the element.

        @param pos An iterator to the element.
    */</doc>
<def f='boost-1.85.0/libs/url/src/params_ref.cpp' l='155' ll='170' type='boost::urls::params_base::iterator boost::urls::params_ref::unset(boost::urls::params_base::iterator pos)'/>
