<def f='boost-1.85.0/libs/intrusive/include/boost/intrusive/slist.hpp' l='369' ll='375' type='void boost::intrusive::slist_impl::~slist_impl&lt;ValueTraits, SizeType, BoolFlags, HeaderHolder&gt;()'/>
<doc f='boost-1.85.0/libs/intrusive/include/boost/intrusive/slist.hpp' l='360'>//! &lt;b&gt;Effects&lt;/b&gt;: If it&apos;s a safe-mode
   //!   or auto-unlink value, the destructor does nothing
   //!   (ie. no code is generated). Otherwise it detaches all elements from this.
   //!   In this case the objects in the list are not deleted (i.e. no destructors
   //!   are called), but the hooks according to the value_traits template parameter
   //!   are set to their default value.
   //!
   //! &lt;b&gt;Complexity&lt;/b&gt;: Linear to the number of elements in the list, if
   //!   it&apos;s a safe-mode or auto-unlink value. Otherwise constant.</doc>
