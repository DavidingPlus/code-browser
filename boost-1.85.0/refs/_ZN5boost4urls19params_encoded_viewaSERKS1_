<def f='boost-1.85.0/libs/url/include/boost/url/params_encoded_view.hpp' l='190' ll='192' type='boost::urls::params_encoded_view &amp; boost::urls::params_encoded_view::operator=(const boost::urls::params_encoded_view &amp; )'/>
<doc f='boost-1.85.0/libs/url/include/boost/url/params_encoded_view.hpp' l='168'>/** Assignment

        After assignment, both views
        reference the same underlying character
        buffer.

        Ownership is not transferred; the caller
        is responsible for ensuring the lifetime
        of the buffer extends until it is no
        longer referenced.

        @par Postconditions
        @code
        this-&gt;buffer().data() == other.buffer().data()
        @endcode

        @par Complexity
        Constant

        @par Exception Safety
        Throws nothing
    */</doc>
