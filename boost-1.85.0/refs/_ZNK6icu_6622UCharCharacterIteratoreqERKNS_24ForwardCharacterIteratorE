<dec f='include/unicode/uchriter.h' l='122' type='UBool icu_66::UCharCharacterIterator::operator==(const icu_66::ForwardCharacterIterator &amp; that) const'/>
<doc f='include/unicode/uchriter.h' l='114'>/**
   * Returns true if the iterators iterate over the same range of the
   * same string and are pointing at the same character.
   * @param that The ForwardCharacterIterator used to be compared for equality
   * @return true if the iterators iterate over the same range of the
   * same string and are pointing at the same character.
   * @stable ICU 2.0
   */</doc>
