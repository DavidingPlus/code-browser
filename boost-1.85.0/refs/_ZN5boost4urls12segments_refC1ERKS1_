<def f='boost-1.85.0/libs/url/include/boost/url/segments_ref.hpp' l='124' ll='125' type='void boost::urls::segments_ref::segments_ref(const boost::urls::segments_ref &amp; other)'/>
<doc f='boost-1.85.0/libs/url/include/boost/url/segments_ref.hpp' l='102'>/** Constructor

        After construction, both views
        reference the same url. Ownership is not
        transferred; the caller is responsible
        for ensuring the lifetime of the url
        extends until it is no longer
        referenced.

        @par Postconditions
        @code
        &amp;this-&gt;url() == &amp;other.url();
        @endcode

        @par Complexity
        Constant.

        @par Exception Safety
        Throws nothing.

        @param other The other view.
    */</doc>
