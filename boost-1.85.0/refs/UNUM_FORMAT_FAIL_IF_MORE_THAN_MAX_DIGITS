<dec f='include/unicode/unum.h' l='1067' type='4096'/>
<doc f='include/unicode/unum.h' l='1062'>/** If 1, specifies that if setting the &quot;max integer digits&quot; attribute would truncate a value, set an error status rather than silently truncating.
   * For example,  formatting the value 1234 with 4 max int digits would succeed, but formatting 12345 would fail. There is no effect on parsing.
   * Default: 0 (not set)
   * @stable ICU 50
   */</doc>
