<dec f='include/unicode/numfmt.h' l='512' type='icu_66::UnicodeString &amp; icu_66::NumberFormat::format(int64_t number, icu_66::UnicodeString &amp; appendTo, icu_66::FieldPosition &amp; pos) const'/>
<doc f='include/unicode/numfmt.h' l='499'>/**
     * Format an int64 number. (Not abstract to retain compatibility
     * with earlier releases, however subclasses should override this
     * method as it just delegates to format(int32_t number...);
     *
     * @param number    The value to be formatted.
     * @param appendTo  Output parameter to receive result.
     *                  Result is appended to existing contents.
     * @param pos       On input: an alignment field, if desired.
     *                  On output: the offsets of the alignment field.
     * @return          Reference to &apos;appendTo&apos; parameter.
     * @stable ICU 2.8
    */</doc>
