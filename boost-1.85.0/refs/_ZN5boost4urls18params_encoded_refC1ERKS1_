<def f='boost-1.85.0/libs/url/include/boost/url/params_encoded_ref.hpp' l='112' ll='113' type='void boost::urls::params_encoded_ref::params_encoded_ref(const boost::urls::params_encoded_ref &amp; other)'/>
<doc f='boost-1.85.0/libs/url/include/boost/url/params_encoded_ref.hpp' l='90'>/** Constructor

        After construction, both views
        reference the same url. Ownership is not
        transferred; the caller is responsible
        for ensuring the lifetime of the url
        extends until it is no longer
        referenced.

        @par Postconditions
        @code
        &amp;this-&gt;url() == &amp;other.url();
        @endcode

        @par Complexity
        Constant.

        @par Exception Safety
        Throws nothing.

        @param other The other view.
    */</doc>
