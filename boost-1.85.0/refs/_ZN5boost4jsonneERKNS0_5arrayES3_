<def f='boost-1.85.0/libs/json/include/boost/json/array.hpp' l='1657' ll='1664' type='bool boost::json::operator!=(const boost::json::array &amp; lhs, const boost::json::array &amp; rhs)'/>
<doc f='boost-1.85.0/libs/json/include/boost/json/array.hpp' l='1641'>/** Return `true` if two arrays are not equal.

        Arrays are equal when their sizes are
        the same, and they are element-for-element
        equal in order.

        @par Effects
        `return ! std::equal( lhs.begin(), lhs.end(), rhs.begin(), rhs.end() );`

        @par Complexity
        Constant or linear in `lhs.size()`.

        @par Exception Safety
        No-throw guarantee.
    */
    // inline friend speeds up overload resolution</doc>
