<def f='boost-1.85.0/libs/intrusive/include/boost/intrusive/slist.hpp' l='1023' ll='1024' type='boost::intrusive::slist_impl::iterator boost::intrusive::slist_impl::erase(boost::intrusive::slist_impl::const_iterator f, boost::intrusive::slist_impl::const_iterator l, boost::intrusive::slist_impl::size_type n)'/>
<doc f='boost-1.85.0/libs/intrusive/include/boost/intrusive/slist.hpp' l='1009'>//! &lt;b&gt;Effects&lt;/b&gt;: Erases the range [f, l) from
   //!   the list. n must be distance(f, l).
   //!   No destructors are called.
   //!
   //! &lt;b&gt;Returns&lt;/b&gt;: the first element remaining beyond the removed elements,
   //!   or end() if no such element exists.
   //!
   //! &lt;b&gt;Throws&lt;/b&gt;: Nothing.
   //!
   //! &lt;b&gt;Complexity&lt;/b&gt;: linear to the elements before f if link_mode is normal_link
   //!   and constant_time_size is activated. Linear to the elements before l otherwise.
   //!
   //! &lt;b&gt;Note&lt;/b&gt;: Invalidates the iterators (but not the references) to the
   //!   erased element.</doc>
