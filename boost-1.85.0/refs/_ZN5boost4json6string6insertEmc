<def f='boost-1.85.0/libs/json/include/boost/json/string.hpp' l='1606' ll='1612' type='boost::json::string &amp; boost::json::string::insert(boost::json::string::size_type pos, char ch)'/>
<doc f='boost-1.85.0/libs/json/include/boost/json/string.hpp' l='1584'>/** Insert a character.

        Inserts the character `ch` before the character
        at index `pos`.

        @par Exception Safety

        Strong guarantee.

        @note All references, pointers, or iterators
        referring to contained elements are invalidated.
        Any past-the-end iterators are also invalidated.

        @return `*this`

        @param pos The index to insert at.

        @param ch The character to insert.

        @throw `boost::system::system_error` `size() + 1 &gt; max_size()`.
        @throw `boost::system::system_error` `pos &gt; size()`.
    */</doc>
